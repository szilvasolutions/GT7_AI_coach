# Copy this file to .env and fill in the keys for whichever provider(s) you
# plan to use. Only the coach provider you select in config.yaml is required.
# Telemetry never leaves your machine; only event summaries are sent to the LLM.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
