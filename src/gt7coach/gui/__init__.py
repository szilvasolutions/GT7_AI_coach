"""PySide6 GUI for GT7 AI Coach.

This package is only imported when the user runs ``gt7coach-gui``. It
depends on PySide6 (installed via the ``[gui]`` optional extra). The
CLI tools (``gt7coach-coach``, ``gt7coach-capture``, ``gt7coach-setup``)
do not import this package.
"""
